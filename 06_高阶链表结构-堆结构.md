

# 一。高阶链表结构

### 1.单向链表重构

##### 类属性的标识符从private 改为 protected

###### 一共有三种访问修饰符

| 访问修饰符                                 | 本类内部 | 子类内部       | 外部（实例对象） |
| ------------------------------------------ | -------- | -------------- | ---------------- |
| **`private`**                              | 可以访问 | **不可以访问** | 不可以访问       |
| **`protected`**（介于private和public之间） | 可以访问 | **可以访问**   | 不可以访问       |
| public                                     | 可以     | 可以           | 可以             |

##### tail

- append
- traverse
- inserted



### 问题：什么时候要在子类再补充，不大幅度修改父类方法时用

#####  你写完所有代码后，再按照“各司其职”来完善就行



### 报错提示不一定问题就是那一行，可能是别的地方导致，比如下面

```ts
        while(current) { // 不是current.next而是current，否则就少打印一个元素；因为前者时下一个节点存在，后者是当前元素
            arr.push(current.value) // 报错原因时下面的current.Next = null 造成的死循环
            if(this.isTail(current)) {
                current.Next = null
            } else {
                current = current.Next
            }
        }
```







### 2.循环链表基于单向链表通过super实现



### 3.双向链表





# 堆结构

### 必须是完美二叉树

#### 才可以是最大堆/最小堆

#### 但也不一定是完美二叉树才是堆结构 但几乎所有人都遵守，因为遵守进行堆结构操作更方便，比如和使用下面的公式



### 获取节点索引公式（不知道怎么推导，可以问ai或看之前chapgpt记录）

#### 数组下标从 0 开始（一般都是从零开始）

```
parent = math.floor((i - 1) / 2)
left   = 2i + 1
right  = 2i + 2
```

#### 数组下标从 1 开始

```
parent = mathfloor(i / 2) 
left   = 2i
right  = 2i + 1
```

#### 注意parent的索引公式必须有math.floor