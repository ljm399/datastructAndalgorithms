# 为什么别人是ts-node运行，而我是node呢

因为你现在大概率是**直接用 `node` 去执行 `.ts` 文件**，而别人用的是 **`ts-node` 来执行 TypeScript**。

区别很简单：

1. `node`
   只能直接运行 JavaScript，本来**不认识 TypeScript**。
   如果你硬跑 `.ts`，就容易遇到模块解析、扩展名、类型导入这些问题。
2. `ts-node`
   它相当于帮你**边编译边运行 TypeScript**，所以可以直接执行：

```
ts-node xxx.ts
```

不用先手动编译成 `.js`。

你之所以看到报错里是 `node:internal/modules/...`，说明你当前实际运行方式更像是：

```
node 02_实现栈结构_链表.ts
```

或者某个工具底层还是走了 Node 的 ESM 加载流程。

常见情况有这几种：

- 你装的是 Node，但**没装 `ts-node`**
- 你运行命令写的是 `node xxx.ts`
- 你用的编辑器“运行按钮”默认走的是 `node`
- 你项目里配置的是 ESM/Node 直接执行，而不是 `ts-node`

如果你想像别人那样运行 TypeScript，通常要先装：

```
npm install -D ts-node typescript
```

然后运行：

```
npx ts-node 02_实现栈结构_链表.ts
```

如果你是全局装：

```
npm install -g ts-node typescript
ts-node 02_实现栈结构_链表.ts
```

再补一句：

- **学习 TS 时**：`ts-node` 很方便
- **正式项目里**：更常见的是先 `tsc` 编译，再 `node xxx.js`



### 为什么有时候node可以运行ts代码

在你的 Node 里可以，是因为：

1. `import type` 本来就是**纯类型导入**
2. `interface` 也是**纯类型内容**
3. Node 24 会把这些类型相关语法剥掉后再运行

但要注意，`node` 现在不是“完整支持 TypeScript”，只是支持这类轻量场景。像这些就仍然要小心：

- Node **不会帮你做类型检查**
- 不是所有 TS 特性都能直接跑
- 模块路径仍然要按 ESM 规则写清楚扩展名，比如 `./xx.ts`

所以你和别人并不是完全不同，只是两种运行方式：

- `node`：现在能直接跑一部分 `.ts`
- `ts-node`：更像专门的 TypeScript 运行器，支持更完整

一句话说清楚：
**你能用 `node` 跑，是因为你现在的 Node 版本已经内置了 TypeScript 的“类型擦除运行”；不是因为 `node` 彻底变成了 `ts-node`。**



### 使用ts-node出现问题

#### 配置tsconfig.json

- 可以配置在你项目的根目录，那样你在项目无论那个路径都可以用到这个文件

  ```json
  {
    "compilerOptions": {
      "target": "ES2020",
      "module": "NodeNext",
      "moduleResolution": "NodeNext",
      "allowImportingTsExtensions": true,//关键是这个
      "strict": true
    }
  }
  ```

  `allowImportingTsExtensions: true`
  直接解决你这次的 `.ts` 扩展名导入报错

  ```
  module: "NodeNext"`
  让 TypeScript 按照 Node 现代模块规则处理 `import/export
  ```

  `moduleResolution: "NodeNext"`
  让 TypeScript 按 Node 的模块查找方式解析路径

  `target: "ES2020"`
  指定编译输出面向的 JS 版本，和这次报错关系不大

  `strict: true`
  开启更严格的类型检查，和这次导入报错也没直接关系





# 受限的数据结构如队列，栈

### 作用：对特性的问题有奇效





# 接口为什么有限属性要用get

`get` 适合“像属性一样读取”的数据，尤其是 `size` 这种不是动作，而是“当前状态”。

#### 使用getter属性不要加括号

区别很直接：

```
stack.size()   // 像在执行一个动作
stack.size     // 像在读取一个属性，getter调用不要加括号
```

对 `size` 来说，第二种更自然，因为它只是返回当前长度，不需要传参，也不会产生副作用。

你可以这样理解：

- `push()`、`pop()`、`enqueue()`、`dequeue()` 是“做事”，适合方法
- `size`、`length`、`name` 是“取值”，适合属性或 `getter`



### 使用

如果你把接口从方法：

```ts
size(): number
```

改成访问器属性：

```ts
get size(): number
```

那实现类就不能再写成普通方法了，必须改成 `getter`。

比如你的队列类现在是：

```ts
size(): number {
    return this.data.length
}
```

要改成：

```ts
get size(): number {
    return this.data.length
}
```

#### 调用

- 要从 类名.size() 变为 类目.size  即没有括号





# vscode里面的快速修复可以帮你快速补充类和缺失的接口方法





# ts的 as  和 ！

```
as string`：强行断言成 `string
```

`!`：强行断言“这里不是 `undefined/null`”

对 `dequeue()` 这种场景，`!` 语义更准确

但两者都不安全，本质上都只是“骗过 TypeScript”，不会帮你做运行时保护



# 类里面的constructor简写

```ts
原来
class node<T> {
    Next: node<T> | null = null
    value: T
    constructor(value: T) {
        this.value = value
    } // 也可以时用js语法
}

简写
class node<T> {
    Next: node<T> | null = null
    constructor(public value:T) {}
}
```



# ts中？和 ！作用，以及类型符号

```js
!value          // 逻辑非：true/false 取反
value!          // 非空断言：仅 TypeScript 类型检查有效
obj?.name       // 可选链：obj 为空时结果是 undefined，不报错
a ?? b          // a 是 null/undefined 时，使用 b
x: string       // 类型标注
A | B           // 联合类型：A 或 B
A & B           // 交叉类型：同时满足 A 和 B
value as Type   // 类型断言
```

```js
current!.Next // 断言 current 一定存在；不存在会运行时报错
current?.Next // current 不存在时返回 undefined
```

### current!.Next

告诉编译器：`current` 此处一定不是 `null` 或 `undefined`，允许访问 `.Next`。

它只消除编译报错，运行时不检查；若 `current` 真是 `null`，仍会报错。因此循环中通常应确保条件成立





# 在赋值前被使用”

```js
let previous : node<T> | null// 这里没有复制就使用
privious.next = xx 
```

- 解决

  ```js
  let previous : node<T> | null = null
  privious.next = xx 
  ```

  





#   removeat(position:number): T | null 

- 报错因为要返回值
  - return null 而不是 false那些



#       while(index++<position && current) { //这个包括index=0吗，还是从index=1开始

`index++` 是“先比较，再自增”。

例如 `position = 3`：

| 判断时 index | 比较            | 进入循环后 index |
| ------------ | --------------- | ---------------- |
| 0            | `0 < 3`，成立   | 1                |
| 1            | `1 < 3`，成立   | 2                |
| 2            | `2 < 3`，成立   | 3                |
| 3            | `3 < 3`，不成立 |                  |



#       newNode.Next = previous?.Next ?? null // 为什么这里需要？？null

- 因为previous要是为null，则previous?.Next为undefined，则会导致报错，所以？？null意思是当previous?.Next为undefined时，则转为null





# boolean会把那些值转为false呢

```ts
Boolean(false)      // false
Boolean(0)          // false
Boolean(-0)         // false
Boolean(0n)         // false，BigInt 的 0
Boolean("")         // false，空字符串
Boolean(null)       // false
Boolean(undefined)  // false
Boolean(NaN)        // false
```

```
Boolean("0")        // true
Boolean("false")    // true
Boolean([])         // true，空数组
Boolean({})         // true，空对象
Boolean(-1)         // true
Boolean(" ")        // true，含空格的字符串
```





# 类里面的三个 `value`的区别：

```
class Node<T> {
  value: T | null       // ① 对象属性
  next: Node<T> | null = null

  constructor(value: T) { // ② 构造函数的临时参数
    this.value = value    // ③ 左边是①，右边是②
  }
}
```

① `value: T | null`

声明每个 `Node` 对象都有一个 `value` 属性，可以存 `T` 类型的值或 `null`。

② `constructor(value: T)` 中的 `value`

这是创建节点时传进来的临时变量，只在**构造函数内部**可用。

③ `this.value = value`

- `this.value`：当前创建出来的节点对象的属性，也就是①
- `value`：构造函数接收到的参数，也就是②

例如：

```
const node1 = new Node<number>(100)
```

创建时，参数 `value` 即2的value暂时等于 `100`；随后执行：

```
this.value = value
```

也就是把 `100` 存到新对象的属性中。最终：

```
node1.value // 100
node1.next  // null
```

为了不混淆，通常可以改名：

```
constructor(initialValue: T) {
  this.value = initialValue
}
```

含义完全相同。





# 解释类里面的属性在其构造函数里面赋值和不在里面赋值的区别

```ts
// class node<T> {
//     value:T|null
//     next:node<T> | null = null
//     constructor(value:T) { 
//         this.value = value
//     }
// }

上下这两个有关next的赋值有什么区别吗

class node<T> {
    value:T|null
    next:node<T> | null 
    constructor(value:T,next:node<T>) { // 为什么
        this.value = value,
        this.next = next
    }
}
```

有区别，核心在于：**`next` 是不是允许“不传”**，以及默认值是什么。

第一种：

```
next: node<T> | null = null
constructor(value: T) {
  this.value = value
}
```

意思是：

- 创建节点时**只需要传** `value`
- `next` 默认就是 `null`

例如：

```
const a = new node(1)
// a.next === null
```

第二种：

```
next: node<T> | null
constructor(value: T, next: node<T>) {
  this.value = value
  this.next = next
}
```

意思是：

- 但构造函数参数 `next` **只允许传** `node<T>`，不允许传 `null`
- 创建节点时**必须传第二个参数**

例如：

```
const b = new node(1) // 报错，缺少 next
const c = new node(1, null) // 报错，因为参数类型不是 node<T>
```

# TypeScript 里类名建议用大写 `Node`，不要用小写 `node`。
