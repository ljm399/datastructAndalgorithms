# 链表面试题

### 1.1.反转链表的三种解决方案

- 利用栈结构
- 循环
- 递归





### 1.2.对链表接口封装和继承







# 二。算法的复杂度

### 2.1.理论相关的复杂度分析





### 2.2.查找算法的性能对比

- 顺序查找
- 二分查找



### 2.3.大O表示法的由来和推导过程

大 O 表示法（Big O Notation）用于描述：当输入规模 `n` 不断增大时，算法运行时间或占用空间的**增长上界**。它关注增长趋势，而不是某一台机器上精确执行了多少毫秒。

例如，顺序查找长度为 `n` 的数组，最坏情况下要比较 `n` 次，记为 `T(n) = an + b`：

- `a` 是每次比较等固定操作的成本；
- `b` 是初始化等与 `n` 无关的成本；
- 当 `n` 足够大时，`an` 主导增长趋势，`b` 可以忽略；
- 常数系数不改变增长阶数，因此 `an + b` 记为 `O(n)`。

推导时通常遵循以下规则：

1. 用 `n` 表示输入规模，写出**基本操作执行次数** `T(n)`。
2. 保留最高阶项：`3n² + 2n + 1` 中，`n²` 增长最快，保留为 `3n²`。
3. 忽略常数系数：`3n²`、`100n²` 都记为 `O(n²)`。
4. 顺序执行的代码块相加：`O(n) + O(n²) = O(n²)`。
5. 嵌套循环通常相乘：外层 `n` 次、内层 `n` 次，得到 `O(n²)`。

```js
// T(n) = 1 + n + n，去掉常数和系数后为 O(n)
function sum(numbers) {
  let total = 0;
  for (const number of numbers) {
    total += number;
  }
  return total;
}

// 外层 n 次，每次内层也执行 n 次：T(n) = n * n，故为 O(n²)
function printPairs(numbers) {
  for (const left of numbers) {
    for (const right of numbers) {
      console.log(left, right);
    }
  }
}
```

大 O 常用于最坏情况分析，因此它便于比较算法的规模适应能力；但它会忽略常数、缓存命中和实现细节。输入规模较小时，`O(n)` 算法不一定总比 `O(n log n)` 算法更快。




### 2.4.常见的阶数的含义（前三个常见）

- O(1)
- O(n)
- O(logn)
- O(nlogn)
- O(n**2)
- ....





### 2.5.空间复杂度的分析

空间复杂度描述算法在运行过程中，随着输入规模 `n` 增长而额外占用的内存趋势，通常记为 `S(n)`。这里主要看**辅助空间**，即除输入数据本身之外新申请的空间；若题目要求，也需要单独说明是否把输出空间计入。

常见判断方法：

- 只使用有限个变量，如计数器、临时变量：`O(1)`。
  - 变量用完就销毁了

- 新建长度与输入成正比的数组、对象或哈希表：`O(n)`。
- 二维 `n × n` 表、邻接矩阵：`O(n²)`。
- 递归还要计算调用栈。递归深度为 `n` 时通常是 `O(n)`，二分递归深度为 `log n` 时通常是 `O(log n)`。
- 递归容易导致空间复杂度增高和栈溢出

```js
// 仅额外使用 total 一个变量：O(1) 空间
function sum(numbers) {
  let total = 0;
  for (const number of numbers) total += number;
  return total;
}

// result 最多保存 n 个元素：O(n) 空间
function doubled(numbers) {
  const result = [];
  for (const number of numbers) result.push(number * 2);
  return result;
}
```

时间复杂度和空间复杂度需要分别分析。用哈希表将查找从线性时间优化到平均 `O(1)` 时，往往会以 `O(n)` 的额外空间为代价，这就是典型的**时间换空间**。

#### 动态优化

- 时间换空间
- 或 空间换时间




### 2.6.数组和链表的效率对比

数组使用一段连续内存存储元素；链表的节点分散存储，每个节点除数据外还保存指向相邻节点的引用。两者的效率差异来自这两种存储方式。

| 操作 | 数组 | 链表 | 原因 |
| --- | --- | --- | --- |
| 按下标**访问**第 `i` 个元素 | `O(1)` | `O(n)` | 数组可按地址偏移定位；链表必须逐节点遍历。 |
| 按值**查找** | `O(n)` | `O(n)` | 都可能需要遍历全部元素。 |
| 已知位置**插入/删除** | `O(n)` | `O(1)` | 数组需要移动后续元素；链表只需修改相邻引用。 |
| 头部插入/删除 | `O(n)` | `O(1)` | 数组元素需要整体移动；链表改头指针即可。 |
| 尾部追加 | 均摊 `O(1)` | `O(1)`（有尾指针） | 动态数组偶尔扩容并复制；链表需维护尾指针。 |
| 内存与缓存局部性 | 较好 | 较差 | 数组连续存储，CPU 缓存更容易命中。 |

“链表插入删除是 `O(1)`”的前提是已经拿到了目标节点（或其前驱节点）。如果先按下标或按值定位节点，定位本身仍然是 `O(n)`，总时间通常也是 `O(n)`。

- 数组：JavaScript 的 **`Array` 适合****频繁按下标访问、遍历和尾部**追加的场景；**

- 链表：**当需要大量在已知**节点附近插入、删除，且能接受节点**引用和额外内存开销时，链表更合适。






# 三。哈希表的理论

### 3.1.什么是哈希表

哈希表（Hash Table）是一种通过“键（key）”快速存取“值（value）”的**数据结构**。它将键经过哈希函数转换为数组下标或桶编号，再把数据放入对应位置。

可以把它理解为一本带索引的字典：不必从第一页逐项翻到目标词，而是先根据词条定位到对应页。理想情况下，插入、查找和删除的平均时间复杂度都是 `O(1)`。

一般通过数组来实现，但其插入、查找和删除的平均时间复杂度都是 `O(1)`,而数组插入和删除是O(n)

```js
const scores = new Map();
scores.set('Alice', 95);
scores.set('Bob', 88);

scores.get('Alice'); // 95
scores.has('Carol'); // false
scores.delete('Bob');
```

JavaScript 的 `Map` 是通用键值映射；普通对象也常用于字符串键的简单映射。两者的底层实现细节由引擎决定，但都提供了哈希表式的按键访问能力。






### 3.2.两个案例选择

- 公司储存员工信息
- 储存500000个单词
  - 每个单词有发音，翻译，例子



### 3.3.字母转数字方案

哈希函数通常先把**键**转换为**数字**，再将数字映射到有限的桶范围。对于只包含小写英文字母的字符串，可以把 `a` 到 `z` 分别映射为 `0` 到 `25`：

```js
function letterToNumber(letter) {
  return letter.charCodeAt(0) - 'a'.charCodeAt(0);
}

letterToNumber('a'); // 0
letterToNumber('z'); // 25
```

单纯把每个字符的数值相加并不可靠，因为不同字符串可能得到相同结果：`"ab"` 和 `"ba"` 都是 `0 + 1`。更常见的方案是把字符串看作以 26 为基数的数字：

```js
function stringToNumber(text) {
  let value = 0;
  for (const letter of text) {
    value = value * 26 + letterToNumber(letter);
  }
  return value;
}

stringToNumber('ab'); // 1
stringToNumber('ba'); // 26
```

这种方式保留了字符顺序，区分度更高。但字符串很长时结果会快速增大，因此还需要进一步压缩到桶的范围内。




### 3.4.大数字的压缩算法

哈希表的桶数量是有限的，例如容量 `size = 11`。将较大的哈希值压缩为合法下标，最常用的是**取模**（除留余数）法：

```js
function compress(hashCode, size) {
  return hashCode % size;
}

compress(123456, 11); // 3，结果始终在 0 到 10 之间
```

完整的字符串哈希通常会在累积过程中**立即取模**（%），避免数值无限增大：

```js
function hash(text, size) {
  let hashCode = 0;
  for (const letter of text) {
    const value = letterToNumber(letter);
    hashCode = (hashCode * 31 + value) % size;
  }
  return hashCode;
}
```

其中 `31` 是一个常见的质数乘数；桶容量也常取质数，以减少某些规律性输入集中到少数桶的概率。压缩只能把值映射到有限范围，不能从根本上避免不同键落入同一桶，所以仍需处理冲突。




### 3.5.常见的哈希表概念

- **哈希化**：将任意类型或任意长度的键转换为固定范围整数的过程。例如把字符串 `"alice"` 转换为桶下标 `4`。
- **哈希函数**：执行哈希化的函数。好的哈希函数应当计算快、结果稳定，并尽量让不同键均匀分布到各个桶中。
- **哈希表**：由桶数组、哈希函数和冲突处理策略组成的键值数据结构。键经哈希函数确定桶位置，再在桶中存储或查找对应键值对。

相关术语：

- **桶（bucket）**：哈希表底层数组中的一个位置，用于容纳一个或多个键值对。
- **哈希值（hash code）**：键经哈希函数计算得到的整数；经压缩后可得到桶下标。
- **装载因子（load factor）**：`元素数量 / 桶数量`。装载因子越高，发生冲突的概率通常越大。
- **冲突（collision）**：不同的键被映射到同一个桶。冲突是正常现象，必须由冲突处理策略保证数据仍可正确读写。





### 3.6.冲突的解决方案

- **链地址法（separate chaining）**：每个桶保存一条链表（也可以是数组），同一桶中的多个键值对依次存放。当键发生冲突时，不覆盖已有元素，而是追加到该桶的链中。

查找时先定位桶，再在该桶的链中比较键。若哈希函数分布均匀，单个桶中的元素数量较少；当装载因子过高时，通常通过扩容并重新哈希（rehash）来恢复效率。





### 3.7.效率的对比（对比）

以下比较假设哈希函数分布较均匀，并以 `n` 表示元素数量。哈希表的平均复杂度很低，但最坏情况仍可能退化。

| 数据结构 | 查找 | 插入 | 删除 | 适用情况 |
| --- | --- | --- | --- | --- |
| 数组（无序） | `O(n)` | 尾部均摊 `O(1)`，中间 `O(n)` | `O(n)` | 顺序遍历、按下标访问。 |
| 链表 | `O(n)` | 已知节点位置时 `O(1)` | 已知节点位置时 `O(1)` | 频繁在已知位置插入、删除。 |
| 哈希表（链地址法） | 平均 `O(1)`，最坏 `O(n)` | 平均 `O(1)`，最坏 `O(n)` | 平均 `O(1)`，最坏 `O(n)` | 按唯一键快速增删查。 |

链地址法的实际效率与装载因子 `α = n / m` 有关，`m` 是桶数量。平均而言，桶内需要比较约 `O(α)` 个元素，因此哈希表操作可视为 `O(1 + α)`；维持较低且稳定的装载因子时，平均性能接近 `O(1)`。

哈希表**不适合**按**大小顺序遍历、范围查询或查找最小/最大键**，因为键的存储顺序不稳定。需要这些能力时，应考虑有序数组、平衡搜索树等有序结构。
