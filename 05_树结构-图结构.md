# 一。树结构

### 1.1.BST的插入操作



### 1.2.BST的遍历操作

- 先序遍历
  - 即根节点放最前面
- 中序遍历
  - 放中间
- 后序遍历
  - 最后
    - 即左右中
- 层序遍历





### 1.3.BST的最值

- 最大值
- 最小值



### 1.4.BST的搜索操作





### 1.5.BST的删除操作

- 情况一：删除叶子节点

- 情况二：有一个子节点
  - 左子节点
  - 右子节点
- 情况三：两个字节的
  - 前驱/后继节点



- 重构代码



### 1.6.二叉搜索树的缺点

- 二叉搜索树的查找效率取决于树的高度
  - 树的结构比较平衡时，高度约为 `log₂n`，插入、删除和查找的平均时间复杂度为 `O(log n)`
  - 树严重倾斜时，高度可能增长到 `n`，这些操作的最坏时间复杂度会退化为 `O(n)`
- 普通二叉搜索树不会主动调整结构
  - 节点的插入顺序不同，最终形成的树也不同
  - 如果数据本身接近有序，树很容易退化成类似链表的结构

例如，依次插入 `10、20、30、40、50`：

```text
10
  \
   20
     \
      30
        \
         40
           \
            50
```

此时查找 `50` 需要从根节点开始逐个比较，二叉搜索树的性能优势基本消失。

| 树的状态 | 树高 | 查找、插入、删除 |
| --- | --- | --- |
| 结构平衡 | `O(log n)` | `O(log n)` |
| 严重倾斜 | `O(n)` | `O(n)` |

> 因此，普通二叉搜索树只能保证操作的平均时间复杂度为 `O(log n)`，不能保证最坏情况下的性能。





### 1.7.平衡二叉树

- 平衡二叉树通过限制或调整树的高度，避免二叉搜索树退化成链表
- 通常讨论的是**平衡二叉搜索树**：它既满足二叉搜索树的大小关系，又能在插入、删除后维持相对平衡
- “平衡”并不一定表示左右子树完全相同，而是树的高度被控制在 `O(log n)` 级别

常见的平衡二叉搜索树：

- AVL 树
  - 任意节点的左右子树高度差不超过 `1`
  - 平衡要求严格，查找效率稳定
- 红黑树
  - 通过节点颜色和一组规则维持近似平衡
  - 平衡要求相对宽松，插入和删除时通常需要更少的调整

#### AVL 树的平衡因子

```text
平衡因子 = 左子树高度 - 右子树高度
```

- 平衡因子为 `-1、0、1`：该节点处于平衡状态
- 平衡因子的绝对值大于 `1`：该节点失衡，需要调整

当插入或删除导致 AVL 树失衡时，可以通过**旋转**恢复平衡：

- LL 型：右旋
- RR 型：左旋
- LR 型：先对左子树左旋，再对失衡节点右旋
- RL 型：先对右子树右旋，再对失衡节点左旋

例如，将上面的倾斜结构调整后可以得到：

```text
       30
      /  \
    20    40
   /        \
 10          50
```

平衡二叉搜索树的高度保持在 `O(log n)`，所以查找、插入和删除的最坏时间复杂度通常都为 `O(log n)`。代价是插入和删除后可能需要旋转、变色等额外调整，结构和实现也比普通二叉搜索树复杂。



### 二叉搜索树（BST）规则：左子树所有节点 < 当前节点 < 右子树所有节点

# 二。图结构

### 1.1.图结构的认识和特性

- **图（Graph）**是一种由**顶点**和**边**组成的非线性数据结构，通常记为：

```text
G = (V, E)

V：顶点（Vertex）的集合
E：边（Edge）的集合
```

- 顶点用于表示对象，边用于表示对象之间的关系。例如：
  - 社交网络：用户是顶点，好友关系是边
  - 地图导航：地点是顶点，道路是边
  - 计算机网络：设备是顶点，网络连接是边
  - 课程依赖：课程是顶点，先修关系是边
- 与树不同，图没有固定的根节点，也不存在天然的父子层级；一个顶点可以与任意多个顶点相连。
- 图中可能存在环，也可能存在互不连通的部分。
- **树是一种特殊的图**：无向、连通且没有环。含 `V` 个顶点的树一定有 `V - 1` 条边。

按边的特性可以把图分为：

- **无向图**：边没有方向，`A -- B` 表示 A 和 B 可以相互到达。
- **有向图**：边具有方向，`A -> B` 不代表 `B -> A`。
- **无权图**：边只表示是否相连。
- **带权图**：边还保存距离、时间、费用等权重。
- **稀疏图**：实际边数远小于顶点之间可能存在的边数。
- **稠密图**：实际边数接近最大边数。

> 简单无向图最多有 `V * (V - 1) / 2` 条边；简单有向图最多有 `V * (V - 1)` 条边。这里不考虑自环和重复边。




### 1.2.图结构的历史：七桥问题

18 世纪的普鲁士柯尼斯堡城被河流分成四块陆地，七座桥连接着这些陆地。问题是：能否从某处出发，**每座桥恰好经过一次**，最后走完所有桥？

欧拉在 1736 年解决了这个问题。他忽略陆地的形状、桥的长度等无关信息，只保留连接关系：

- 每块陆地抽象为一个**顶点**。
- 每座桥抽象为一条**边**。
- 问题转化为：能否在图中找到一条经过每条边恰好一次的路径？

这类路径称为**欧拉路径**；起点和终点相同时称为**欧拉回路**。

对于忽略孤立顶点后连通的无向图：

- 所有顶点的度都是偶数：存在欧拉回路。
- 恰好有两个奇数度顶点：存在欧拉路径，这两个顶点分别是起点和终点。
- 奇数度顶点超过两个：不存在欧拉路径。

七桥问题抽象出的四个顶点的度分别是 `3、3、3、5`，有四个奇数度顶点，所以不可能一次不重复地走完七座桥。

> 七桥问题的重要意义不只是得到“走不通”的答案，而是展示了如何舍弃现实问题中的次要信息，只保留对象和关系。这种抽象促成了图论的诞生。




### 1.3.图结构相关的术语和表示

#### 常用术语

- **相邻（Adjacent）**：两个顶点之间存在边，则称它们相邻。
- **关联（Incident）**：一条边连接某个顶点，则称该边与该顶点关联。
- **度（Degree）**：无向图中，与某个顶点相连的边数。
- **入度（In-degree）**：有向图中，指向某个顶点的边数。
- **出度（Out-degree）**：有向图中，从某个顶点出发的边数。
- **路径（Path）**：由一系列顶点及连接它们的边组成的序列。
- **路径长度**：无权图中通常指边数；带权图中通常指各边权重之和。
- **简单路径**：路径中没有重复顶点。
- **环（Cycle）**：起点和终点相同的路径。
- **连通**：无向图中两个顶点之间存在路径。
- **连通图**：任意两个顶点之间都存在路径的无向图。
- **连通分量**：无向图中彼此连通的最大顶点集合。
- **强连通**：有向图中两个顶点可以沿边的方向相互到达。
- **完全图**：任意两个不同顶点之间都有边。
- **子图**：顶点集合和边集合都是原图对应集合的子集。

> 欧拉路径要求每条**边**恰好经过一次；哈密顿路径要求每个**顶点**恰好经过一次，二者不要混淆。

#### 邻接矩阵

使用二维数组表示顶点之间是否存在边。以下无向图包含 `A-B、A-C、B-D、C-D` 四条边：

```text
    A B C D
A [ 0 1 1 0 ]
B [ 1 0 0 1 ]
C [ 1 0 0 1 ]
D [ 0 1 1 0 ]
```

- `matrix[i][j] = 1` 表示存在边；带权图可以把 `1` 换成权重，不相连的位置用 `Infinity` 等值表示。
- 无向图的邻接矩阵沿主对角线对称；有向图不一定对称。
- 判断两个顶点是否相邻很快，但无论边数多少都需要 `O(V^2)` 空间，更适合稠密图。

#### 邻接表

为每个顶点保存一个相邻顶点集合：

```text
A -> B, C
B -> A, D
C -> A, D
D -> B, C
```

- 空间复杂度为 `O(V + E)`，更适合常见的稀疏图。
- 遍历某个顶点的所有邻居非常方便。
- 无向边会在两个顶点的邻接集合中各保存一次。

| 操作或特点 | 邻接矩阵 | 邻接表（使用 `Set`） |
| --- | --- | --- |
| 空间复杂度 | `O(V^2)` | `O(V + E)` |
| 判断边是否存在 | `O(1)` | 平均 `O(1)` |
| 遍历一个顶点的邻居 | `O(V)` | `O(degree)` |
| 适用场景 | 稠密图 | 稀疏图 |




### 1.4.图结构的封装和操作

- 添加顶点
- 添加边

下面使用 `Map<T, Set<T>>` 实现邻接表：`Map` 保存“顶点 -> 邻居集合”的映射，`Set` 可以避免重复边。

```ts
class Graph<T> {
  private readonly adjacencyList = new Map<T, Set<T>>()

  constructor(private readonly directed = false) {}

  // 添加成功返回 true；顶点已存在时返回 false
  addVertex(vertex: T): boolean {
    if (this.adjacencyList.has(vertex)) return false

    this.adjacencyList.set(vertex, new Set<T>())
    return true
  }

  addEdge(from: T, to: T): void {
    // 边涉及的顶点不存在时自动创建
    this.addVertex(from)
    this.addVertex(to)

    this.adjacencyList.get(from)!.add(to)

    // 无向边需要保存两个方向
    if (!this.directed) {
      this.adjacencyList.get(to)!.add(from)
    }
  }

  hasVertex(vertex: T): boolean {
    return this.adjacencyList.has(vertex)
  }

  hasEdge(from: T, to: T): boolean {
    return this.adjacencyList.get(from)?.has(to) ?? false
  }

  getNeighbors(vertex: T): T[] {
    return [...(this.adjacencyList.get(vertex) ?? [])]
  }

  getVertices(): T[] {
    return [...this.adjacencyList.keys()]
  }

  toString(): string {
    return this.getVertices()
      .map(vertex => `${String(vertex)} -> ${this.getNeighbors(vertex).join(', ')}`)
      .join('\n')
  }
}
```

创建一张无向图：

```ts
const graph = new Graph<string>()

graph.addEdge('A', 'B')
graph.addEdge('A', 'C')
graph.addEdge('B', 'D')
graph.addEdge('B', 'E')
graph.addEdge('C', 'F')
graph.addEdge('E', 'F')

console.log(graph.toString())
```

```text
A -> B, C
B -> A, D, E
C -> A, F
D -> B
E -> B, F
F -> C, E
```

- `new Graph<string>()` 创建无向图。
- `new Graph<string>(true)` 创建有向图，此时 `addEdge('A', 'B')` 只添加 `A -> B`。
- 使用邻接表时，添加顶点的时间复杂度平均为 `O(1)`；使用 `Set` 添加边的时间复杂度平均为 `O(1)`。



### 1.5.图结构的广度优先搜索

**广度优先搜索（Breadth-First Search，BFS）**从起点开始，先访问距离起点最近的顶点，再一层一层向外扩展。其核心数据结构是**队列**。

```text
起点入队并标记为已访问
while 队列不为空：
  顶点出队并访问
  将它所有未访问的邻居标记后入队
```

必须在顶点**入队时**就标记为已访问，否则同一个顶点可能被多个邻居重复加入队列。

```ts
function bfs<T>(graph: Graph<T>, start: T): T[] {
  if (!graph.hasVertex(start)) return []

  const visited = new Set<T>([start])
  const queue: T[] = [start]
  const result: T[] = []
  let front = 0

  while (front < queue.length) {
    const vertex = queue[front++]
    result.push(vertex)

    for (const neighbor of graph.getNeighbors(vertex)) {
      if (visited.has(neighbor)) continue

      visited.add(neighbor)
      queue.push(neighbor)
    }
  }

  return result
}

console.log(bfs(graph, 'A'))
// ['A', 'B', 'C', 'D', 'E', 'F']
```

这里使用 `front` 指针读取队首，而不是反复调用 `shift()`，避免移动数组元素带来的额外开销。

BFS 的特点：

- 在**无权图**中，BFS 首次到达某个顶点时所经过的路径就是边数最少的路径。
- 常用于最短路径、层级遍历、社交关系的“几度好友”和网络爬取。
- 使用邻接表时，时间复杂度为 `O(V + E)`，额外空间复杂度为 `O(V)`。
- 从一个起点只能遍历其所在的连通分量。要遍历非连通图，需要对所有未访问顶点再次执行 BFS。




### 1.6.图结构的深度优先搜索

**深度优先搜索（Depth-First Search，DFS）**会沿一条路径尽可能向深处访问，走不通时再回溯。其核心数据结构是**递归调用栈**或手动维护的栈。

```text
访问当前顶点并标记
依次检查当前顶点的邻居：
  如果邻居未访问，则从该邻居继续深入
```

```ts
function dfs<T>(graph: Graph<T>, start: T): T[] {
  if (!graph.hasVertex(start)) return []

  const visited = new Set<T>()
  const result: T[] = []

  function visit(vertex: T): void {
    visited.add(vertex)
    result.push(vertex)

    for (const neighbor of graph.getNeighbors(vertex)) {
      if (!visited.has(neighbor)) {
        visit(neighbor)
      }
    }
  }

  visit(start)
  return result
}

console.log(dfs(graph, 'A'))
// ['A', 'B', 'D', 'E', 'F', 'C']
```

DFS 的特点：

- 常用于连通性判断、路径查找、环检测、拓扑排序和迷宫回溯。
- 使用邻接表时，时间复杂度为 `O(V + E)`，额外空间复杂度为 `O(V)`。
- 访问顺序不是唯一的，它取决于邻接点的存储顺序，但不会影响“每个可达顶点只访问一次”。
- 递归写法直观，但图非常深时可能导致调用栈溢出，此时可以改用显式栈实现。

BFS 与 DFS 的对比：

| 对比项 | BFS | DFS |
| --- | --- | --- |
| 核心结构 | 队列 | 递归栈或显式栈 |
| 搜索方式 | 按层向外扩展 | 沿路径深入后回溯 |
| 无权图最短路径 | 可以保证 | 不能保证 |
| 常见应用 | 最短路径、层级关系 | 回溯、环检测、拓扑排序 |
| 时间复杂度（邻接表） | `O(V + E)` | `O(V + E)` |




### 1.7.图结构的建模（了解）

图建模就是把实际问题转换成“顶点 + 边”，再选择适合的图算法。通常按以下步骤思考：

1. 确定顶点：问题中的哪些对象或状态需要保留？
2. 确定边：什么条件表示两个顶点之间存在关系或可以发生转移？
3. 确定方向：关系是否双向？单向依赖必须使用有向边。
4. 确定权重：边是否需要表示距离、时间、费用或风险？
5. 选择算法：最终要求的是可达性、最短路径、依赖顺序，还是最低连接成本？

常见模型：

| 实际问题 | 顶点 | 边 | 图的类型 | 常用算法 |
| --- | --- | --- | --- | --- |
| 好友关系 | 用户 | 两人是好友 | 无向图 | BFS、DFS |
| 用户关注 | 用户 | A 关注 B | 有向图 | BFS、强连通分量 |
| 地图导航 | 地点 | 道路 | 带权图 | Dijkstra 等最短路径算法 |
| 课程安排 | 课程 | 先修关系 | 有向无环图（DAG） | 拓扑排序 |
| 计算机网络布线 | 设备 | 可连接线路及成本 | 带权无向图 | 最小生成树 |
| 迷宫 | 可停留的位置 | 两位置可以互相移动 | 无权图 | BFS、DFS |

#### 课程依赖示例

假设“数据结构”需要先学习“JavaScript”，“算法”又需要先学习“数据结构”，可以规定边的方向为“先修课程 -> 后续课程”：

```text
JavaScript -> 数据结构 -> 算法
```

这是一个有向图。如果图中没有环，就可以使用**拓扑排序**得到合法的学习顺序；如果存在环，例如 A 依赖 B、B 又依赖 A，则依赖关系无法完成。

#### 二维网格的图建模

迷宫或棋盘通常没有显式给出图，但可以把每个可通行格子看作顶点，把上下左右能够移动的两个格子之间看作边：

```text
网格中的位置 (row, col)  -> 顶点
上下左右的一次合法移动    -> 边
```

- 只判断是否存在出口，可以使用 BFS 或 DFS。
- 寻找步数最少的出口，可以使用 BFS。
- 如果不同位置的移动代价不同，就变成带权图，需要使用相应的最短路径算法。

> 建模时最关键的是明确边的含义。同一个现实问题采用不同的顶点、边和权重定义，可能需要完全不同的算法。

