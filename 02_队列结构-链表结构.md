# 一。队列结构

### 1.1.认识队列以及特性

- FIFO
  - 所以删除的是第一个元素（栈是最后一个元素）
    - 要是用数组是实现队列，则数组（连续内存）后面的元素都要向前移动
    - 所以用链表结构更好



### 1.2.封装队列结构以及接口

#### 常见方法







### 1.3.实现队列结构的方法





### 1.4.面试题一 - 击鼓传花







### 1.5.面试题二 - 约瑟夫环





# 二。链表结构

### 2.1.链表和数组简单对比

数组和链表都是线性结构，都可以按顺序存储多个元素；区别主要在于元素的存储方式和操作成本。

| 对比项 | 数组（Array） | 链表（Linked List） |
| --- | --- | --- |
| 存储方式 | 元素在内存中通常是连续存放的 | 每个节点分散存放，通过指针（引用）连接 |
| 访问元素 | 可以通过下标直接访问，时间复杂度为 `O(1)` | 必须从头节点依次查找，时间复杂度为 `O(n)` |
| 插入、删除 | 中间位置操作时，后续元素通常需要移动，时间复杂度为 `O(n)` | 已找到目标位置后，只需修改节点链接，时间复杂度为 `O(1)` |
| 空间开销 | 只需保存元素本身 | 每个节点还需要保存指向下一个节点的引用 |
| 扩容 | 容量不足时可能需要申请更大空间并复制元素 | 不需要连续空间，按需创建新节点即可 |

简单来说：**需要频繁通过下标查询时，数组更合适；需要频繁在中间插入或删除元素时，链表更合适。**

> 注意：链表的插入和删除是 `O(1)` 的前提是已经找到了对应节点；如果要先按下标查找位置，总时间仍然可能是 `O(n)`。





### 2.2.认识链表结构和特点





### 2.3.封装链表结构

- Node
- LinkedList



### 2.4.链表中方法的封装







### 2.5.链表的面试题一 - 设计链表





### 2.6.链表的面试题二 - 删除节点





### 2.7.链表的面试题三 - 反转链表

