# 一。AVL树的实现过程

### 1.1.AVLTreeNode节点的旋转

##### 1.1.1.理论相关的内容





##### 1.1.2. 右旋转代码实现

- 处理下面的left left即1.2.1
  - 即三个节点，中间的是中枢（轴），要是这三个节点都在其left则是left（left left）

##### 1.1.3.左旋转代码实现

- 处理下面的right right即1.2.2



### 详细解释left left 和 right right

- 有三个节点
  - root（this) , pivot(转轴/中枢)，pivot的子节点
    - 一般root的isbalance是false，其他两个都是true
  - left，left
    - 即pivot和pivot的子节点得到isLeft（判断当前节点是否左节点）都是true
      - right right同理



### 1.2.四种不同情况下的选择判断(就这四种)

##### 1.2.1.左左情况

- 即三个节点，中间的是中枢（轴），要是这三个节点都在其left则是left



##### 1.2.2.右右情况

- 即三个节点，中间的是中枢（轴），要是这三个节点都在其rigth则是right



##### 1.2.3.左右情况





##### 1.2.4.右左情况



# 能上维基百科的都是好信息



# 设计模式







### 1.3.插入操作的调整

##### 1.3.1.创建AVLTreeNode节点





##### 1.3.2.每个节点都要有父节点





##### 1.3.3.根据插入节点的父节点找到不平衡的节点 进行调整





### 1.4.删除操作的调整

##### 1.4.1.调整父节点的情况（父节点要求依然正确的）





##### 1.4.2.找到要删除的节点，从父节点找不平衡节点





##### 1.4.3.有两个叶子节点，重构代码，并且找到删除节点

#### 为什么要重构：因为不重构则要设置的parent很多

- 重构就是不要以前那种思路，直接拿到 删除节点的value =  后继.value 





### 1.5.添加和删除的平衡性的优化

- 平衡后要不要加break

- insert加break

  - insert要，因为添加节点的树和添加之前（旋转之前）的树高度相同
  - 详细解释：

    - insert后则树高度可能添加+1，然后添加后大概率会触发rebalance（看下面代码），所以一定是平衡过了一次

      - 而平衡一次相当于让其高度减去1，所以需要break

      ```ts
              while(current) {
                  if(!current.isBanlance()) {
                      this.rebalance(current)       
                      break
                  }
                  current = current.parent
              }
      ```

      

- remove不能加break

  - 这里不用，是因为你删除后的节点，无法确认是那个父节点的高度-1,所以不能有break

    ```
            while(current) {
                if(!current.isBanlance()) {
                    this.rebalance(current)       
                }
                current = current.parent
            }
    ```

    






# 二。红黑树

### 2.1.邂逅红黑树和红黑树的性质（重要）

- 红黑树（Red-Black Tree）是一种自平衡的**二叉搜索树**。每个节点都带有一个颜色：**红色或黑色**。
- 记空节点（`NIL`就是null）为叶子节点。
- 5条性质：
  1. **每个节点要么是红色，要么是黑色**。
  2. **根节点必须是黑色**。
  3. 所有**叶子节点**（`NIL` 空节点）都是**黑色**。
  4. **红色节点的两个子节点必须都是黑色，也就是不能出现两个连续的红色节点**。
  5. 从**任意节点**出发，到它所有后代 **`NIL` 叶子**的路径，都包含**相同数量的黑色节点**。这个数量称为该节点的**黑高**（black-height）。
- 红黑树**首先**满足**二叉搜索树的性质**：左子树所有节点的值小于当前节点，右子树所有节点的值大于当前节点。因此查找、插入和删除后，还需要通过变色和旋转恢复上面 5 条性质。


### 2.2.红黑树如何让树保存平衡（相对平衡，重要）

- **最长路径一定不要超过最短路径的2倍**
  - 推导过程

    1. **设**从某个节点到 `NIL` 叶子的黑色节点数量为 `bh`。根据性质 5，每一条路径都有相同的 `bh` 个黑色节点。
    2. 最短路径可以只经过黑色节点，因此它的长度至少是 `bh`。
    3. 根据性质 4，红色节点不能连续出现，所以一条路径中红色节点最多只能插在黑色节点之间（或黑色节点之后）。最长路径最多由“黑、红、黑、红……”交替组成，长度至多约为 `2bh`。
    4. 因此，最长路径长度不会超过最短路径长度的 2 倍。红黑树**不是严格平衡**，而是**通过限制红色节点**的连续出现来保证相对平衡。

### 2.3.红黑树代码的实现过程（后续给代码笔记）

- 定义红黑树的节点：定义一个带有键，值，颜色，左子节点，右子节点和父节点的类
- 实现左旋转操作：将一个节点向左旋转，保持红黑树的性质
- 实现右旋操作：将一个节点向右旋转，保持红黑树的性质
- 实现插入操作：再红黑树中插入一个新节点，并保持红黑树的性质
- 实现删除操作：从红黑树中删除一个节点，并保持红黑树的性质
- 实现修复红黑树性质：在插入或删除操作后，通过旋转和变色来修复红黑树的性质
- 其他方法较为简单，可以自行实现



### 2.4.红黑树的性能分析和AVL树的区别

- AVL搜索效率高
  - 因为 AVL 树的**平衡性更严格**，左右子树高度差**最多为 1**，所以同样节点数下**树高通常更低**，**搜索访问的节点相对更少**。
- 红黑树插入/删除效率更高
  - 红黑树牺牲了一点搜索性能，换取更宽松的平衡条件。插入和删除时通常需要的旋转次数更少，频繁修改数据时效率更高。
- 复杂度对比：

  | 操作 | AVL 树 | 红黑树 |
  | --- | --- | --- |
  | 搜索 | `O(log n)`，通常更快 | `O(log n)` |
  | 插入 | `O(log n)`，可能需要多次回溯调整 | `O(log n)`，旋转和调整通常较少 |
  | 删除 | `O(log n)`，维护严格平衡的代价较高 | `O(log n)`，通常更适合频繁删除 |

- 选择建议：查询远多于修改、比较关注搜索延迟时可以选择 AVL；插入和删除频繁、希望整体更新开销稳定时通常选择红黑树。两者的渐进复杂度都是 `O(log n)`，区别主要在平衡严格程度和常数开销。
