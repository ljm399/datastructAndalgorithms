# 你晚上很久没有复习过了 - 重复旧习惯

# log已经是最优算法了

# 实例代码都是以最大堆为标准

### 最小堆逻辑一样



# 一。堆结构的封装

### 堆结构是求最值效率最高的数据结构

### 1.1.插入操作insert（上滤）

#### 优先看对应的代码

堆通常使用数组保存完全二叉树。以最大堆为例（最小堆则下面的条件判断大小相反就行），任意节点都必须满足：父节点的值大于或等于子节点的值。插入新元素时，先放到数组末尾（完全二叉树的下一个空位），再通过“上滤”（sift up）恢复最大堆特性。

#### 插入步骤

1. 将新元素直接添加到数组最后位置：

   ```ts
   data.push(67)
   ```

2. 检查新元素是否满足最大堆特性，并对它执行上滤：

   - 新元素当前索引为 `index = data.length - 1`。
   - 父元素索引为 `parentIndex = Math.floor((index - 1) / 2)`。
   - 比较新元素和父元素：
     - 如果 `data[index] <= data[parentIndex]`，说明堆序已经满足，结束循环。
     - 如果 `data[index] > data[parentIndex]`，交换两个元素，然后令 `index = parentIndex`，继续向上比较。
   - 当新元素到达根节点（`index === 0`）时，没有父节点，循环结束。课件中常将结束条件记作 `index < 0`，但数组根节点的索引是 `0`，实现时使用 `index > 0` 作为循环条件更准确，也能避免计算根节点的父索引。

每次交换都会让元素向上移动一层，最多移动树高 `⌊log₂n⌋` 层，因此插入操作的时间复杂度为 `O(log n)`，额外空间复杂度为 `O(1)`（不计数组扩容）。最小堆的插入过程相同，只需将比较条件改为“子节点小于父节点时交换”。


#### 数据结构可视化网站推荐

- [Algorithms Visualization（美国加利福尼亚州的旧金山大学）](https://www.cs.usfca.edu/~galles/visualization/Algorithms.html)
- [VisuAlgo - Heap](https://visualgo.net/en/heap?slide=1)




### 1.2.删除操作extract（下滤）





### 1.3.原地建堆（buildHeap）





### 1.4.最大堆和最小堆





# 二。高阶队列结构

### 2.1.双向队列



### 2.2.优先级队列

##### 使用堆可以效率更高

- 可以使用队列或栈，但效率没堆高
- 堆则使用最大堆
  - 则优先级通过最大堆的数值体现



# 三。平衡二叉搜索树

### 3.1.介绍平衡树

- 普通二叉搜索树（BST）的操作效率取决于树高。
  - 结构较均匀时，树高约为 `log₂n`，查找、插入和删除的平均时间复杂度为 `O(log n)`。
  - 数据接近有序时，BST 可能退化成链表，树高变成 `n`，操作的最坏时间复杂度退化为 `O(n)`。
- **平衡二叉搜索树**既遵守 BST 的大小关系，又会在插入、删除后主动调整结构，将树高控制在 `O(log n)`。
- “平衡”通常不是指每个节点的左右子树完全一样，而是通过某种规则限制树的倾斜程度。不同平衡树采用的规则不同。

```text
普通 BST 退化：              调整后的平衡结构：

10                                  30
  \                                /  \
   20                            20    40
     \                          /        \
      30                      10          50
        \
         40
           \
            50
```

平衡二叉搜索树需要始终维护两个条件：

1. **有序性**：任意节点的左子树都小于该节点，右子树都大于该节点（重复值如何处理由具体实现决定）。
2. **平衡性**：树高受到约束，不能无限向某一侧倾斜。

常见的平衡二叉搜索树有 AVL 树、红黑树和伸展树（Splay Tree）。另外，B 树、B+ 树也是常见的平衡搜索树，但它们属于多路搜索树，不是二叉树。

> 平衡二叉搜索树不等于完全二叉树或满二叉树。完全二叉树描述节点的排列方式，平衡二叉搜索树关注的是树高和搜索效率。




### 3.2.介绍AVL树以及特性

AVL 树由 Adelson-Velsky 和 Landis 提出（AVL就是其名子开头缩写），是一种**严格平衡的二叉搜索树**。它在满足 BST 性质的基础上，还要求任意节点的左右子树高度差不超过 `1`。

#### 平衡因子

```text
节点的平衡因子 BF = 左子树高度 - 右子树高度
```

- `BF = 0`：左右子树等高。
- `BF = 1`：左子树比右子树高一层。
- `BF = -1`：右子树比左子树高一层。
- `|BF| > 1`：节点失衡，需要通过旋转恢复平衡。
- 空树高度可定义为 `0`，叶子节点高度为 `1`。也可以把空树定义为 `-1`、叶子定义为 `0`，但同一份代码必须统一标准。

插入或删除节点后，沿着该节点到根节点的路径向上检查。遇到失衡节点时，根据“失衡节点较高的一侧”和“较高子节点较高的一侧”确定旋转类型：

| 失衡类型 | 较高路径的方向 | 调整方式 |
| --- | --- | --- |
| LL | 左子树的左侧 | 对失衡节点右旋 |
| RR | 右子树的右侧 | 对失衡节点左旋 |
| LR | 左子树的右侧 | 先对左子节点左旋，再对失衡节点右旋 |
| RL | 右子树的左侧 | 先对右子节点右旋，再对失衡节点左旋 |

以 LL 型为例，对节点 `30` 右旋：

```text
旋转前：               旋转后：

      30                    20
     /                     /  \
   20                    10    30
  /
10
```

旋转只改变局部节点之间的连接关系，不会破坏二叉搜索树的中序有序性。

#### AVL 树的主要特性

- 查找、插入、删除的最坏时间复杂度都是 `O(log n)`。
- 一次插入最多只需在首个失衡位置进行一次单旋或双旋，但仍要沿路径更新节点高度。
- 删除可能让多个祖先节点连续失衡，因此可能需要一路向上进行多次调整。
- 节点通常需要额外保存 `height`（高度）或 `balanceFactor`（平衡因子），也可以动态计算，但动态递归计算的成本更高。

#### AVL 树与红黑树

| 对比项 | AVL 树 | 红黑树 |
| --- | --- | --- |
| 平衡程度 | 更严格，左右子树高度差不超过 `1` | 相对宽松，保证近似平衡 |
| 树高 | 通常更低 | 通常略高 |
| 查找 | 通常更有优势 | 也能保证 `O(log n)` |
| 插入、删除 | 可能进行更多平衡调整 | 通常调整次数更少 |
| 常见场景 | 查找多、更新少 | 插入删除频繁的通用容器 |

> AVL 树与红黑树的查找、插入、删除在渐进复杂度上都是 `O(log n)`。“AVL 查找更快、更新更慢”描述的是常见的常数开销和使用倾向，并不是所有数据与实现下都必然如此。

### 3.3.分析封装AVL树的过程

封装 AVL 树时，可以在已有 **BST 的基础上**增加“**高度信息**”和“**失衡后的旋转**”。**核心目标**是：所有公开操作完成后，BST 有序性、父子引用和 AVL 平衡性必须同时成立。

#### 1.封装节点 `AVLTreeNode`

节点至少需要保存：

- `value`：节点值。
- `left`、`right`：左右子节点。
- `parent`：父节点，便于插入或删除后向上回溯。
- `height`：以当前节点为根的子树高度，叶子节点初始为 `1`。

节点还可以提供以下辅助能力：

- 获取左右子树高度。
- 更新自身高度：`height = max(leftHeight, rightHeight) + 1`。
- 获取平衡因子：`leftHeight - rightHeight`。
- 判断自身是否平衡。
- 获取更高的子节点，用于判断 LL、LR、RR、RL 类型。

#### 2.封装树 `AVLTree`

树类主要负责：

- 保存 `root` 根节点和比较函数 `compareFn`。
- 复用 BST 的查找、遍历、最值等操作。
- 插入或删除后，从结构发生变化的位置开始向上检查平衡。
- 完成左旋、右旋，并正确连接祖父节点、父节点和子节点。

建议将公共逻辑拆成几个私有方法：

```ts
updateHeight(node)       // 根据左右子树更新节点高度
getBalanceFactor(node)   // 计算平衡因子
tallerChild(node)        // 获取较高的子节点
rotateLeft(node)         // 左旋
rotateRight(node)        // 右旋
rebalance(node)          // 判断失衡类型并选择旋转方式
```

#### 3.插入后的再平衡

```text
按 BST 规则插入新节点
        ↓
从新节点的父节点开始向上回溯
        ↓
更新当前节点的高度
        ↓
检查平衡因子是否超出 [-1, 1]
        ↓
若失衡，则判断 LL、LR、RR、RL 并旋转
        ↓
继续向上更新高度，直到根节点
```

判断旋转类型有两种常见方式：

- 根据插入值与失衡节点、子节点的大小关系判断。
- 连续获取两次较高子节点：失衡节点为 `grand`，较高子节点为 `parent`，`parent` 的较高子节点为 `node`，再根据三者方向判断。这种方式也更容易复用于删除操作。

#### 4.旋转时必须维护的引用

以左旋为例，不能**只**交换两个节点，还需要依次处理：

1. **记录**失衡节点**原来的父节点**以及它是**左子节点**还是**右子节点**。
2. 让右子节点成为这棵局部子树的新根。
3. 将右子节点原来的左子树移交给失衡节点的右侧。
4. 更新所有受影响节点的 `parent`。
5. 将局部子树的新根接回原祖父节点；如果原失衡节点就是根，则更新 `root`。
6. 先更新较低节点的高度，再更新新的局部根节点高度。

左右旋转互为镜像。双旋可以复用单旋：LR 是“先左旋左子节点，再右旋失衡节点”，RL 则相反。

#### 5.删除后的再平衡

先按照 BST 的三种情况完成删除：叶子节点、只有一个子节点、拥有两个子节点。若有两个子节点，可以先用前驱或后继节点的值替换待删除节点，再转化为删除至多有一个子节点的情况。

删除后从实际被移除节点的父节点开始向上执行：

```text
更新高度 -> 检查平衡 -> 必要时旋转 -> 继续检查祖先
```

与插入不同，删除一次节点后可能导致多个祖先节点依次失衡，因此旋转一次后不能立即停止。

#### 6.验证封装是否正确

每次插入、删除或旋转后，至少验证：

- 中序遍历结果保持有序，说明 BST 性质没有被破坏。
- 每个节点的 `parent` 与父节点的 `left`、`right` 能够相互对应。
- 每个节点缓存的高度都满足 `max(leftHeight, rightHeight) + 1`。
- 每个节点的平衡因子都属于 `-1、0、1`。
- 分别用会触发 LL、LR、RR、RL 的数据测试四类旋转，并额外测试根节点旋转和连续删除。




### 3.4.AVLTreeNode封装过程



