# 一。邂逅数据结构和算法

### 1.1.编程尽头，数据结构





### 1.2.实际的应用场景

- 源码：Vue/React/Webpack/java。。。
- 大厂面试



### 1.3.如何学习数据结构和算法

- 优质文章
- 看书
- leetcode
- 视频课程



# 二。线性结构 - 数组

### 2.1.什么是线性结构

- 线性结构中的数据元素是**一对一**的关系

  - 一对一：任意一个元素，最多只能跟着一个元素；同时最多只能被一个元素跟着。

- 且除了第一个和最后一个元素，其他元素都同时有**前驱**和**后继**

- 常见的线性结构：
  - 数组
  - 链表
  - 栈
  - 队列

- 注意区分：**线性结构 ≠ 线性表**

  - 线性表（顺序表、链表）：完整满足上面整条规则

  - 特殊例子：**只有 1 个元素的线性结构** 既无前驱，也无后继，依然属于一对一的线性关系。

  - 对比记忆

    - 线性结构：**一对一**

    - 树形结构：**一对多**

    - 图形结构：**多对多**

- 非线性结构则不是这种“一条线”的关系，比如树、图

例如：

- `[10, 20, 30, 40]` 就是典型的线性结构
- `20` 的前面是 `10`，后面是 `30`
- 每个元素之间的关系都很清楚


### 2.2. 数组 - 连续的内存

- 数组在内存中通常是**连续存储**的
- 正因为地址连续，所以数组可以通过**下标**快速找到对应元素
- 这也是数组查询快的根本原因

这样的随机访问，而且速度非常快，时间复杂度通常是 `O(1)`

但是数组也有代价：

- 如果在中间插入一个元素，后面的元素可能都要整体后移
- 如果在中间删除一个元素，后面的元素可能都要整体前移

所以数组的特点可以简单记成：

- **查询快**
- **中间插入、删除相对慢**




# 三。栈结构 - stack

### 3.1.栈结构的认识和特性

- 先进后出
- 后进后出



### 3.2.栈结构的特性 - 笔试题

- 栈是一种**受限的线性结构**
- 只能在同一端进行操作
- 这一端叫做**栈顶**
- 核心特点：**后进先出（LIFO，Last In First Out）**

常见操作：

- `push`：入栈
- `pop`：出栈
- `peek/top`：查看栈顶元素




### 3.3.栈结构的封装和实现



### 3.4.栈结构的代码重构

- 泛型
- 接口设计





### 3.5.面试题一 - 十进制转二进制





### 3.6.面试题二 - 有效的括号



