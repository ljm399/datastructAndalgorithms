# 解释霍纳法则以及对应的实现代码

```ts
function hashFunction(val:string,max:number):number {
    // 1.设置第一个哈希值，必须是0，因为霍纳法则第一项是0
    let hashcode = 0
    const length = val.length
    for(let i=0;i<length;i++) {
        hashcode = hashcode*i + val.charCodeAt(i)
    }
    // % hashcode的范围锁定在数组的长度即max之内
    const index = hashcode % max
    return index
}
```

解释代码

```ts
hashcode = hashcode*i + val.charCodeAt(i)
```

- 解释

假设有一个多项式：

a4x<sup>4</sup>+a3x<sup>3</sup>+a2x<sup>2</sup>+a1x+a0 

普通计算方式是：

a4x<sup>4</sup>+a3x<sup>3</sup>+a2x<sup>2</sup>+a1x+a0

霍纳法则把它改写成：即O(n<sup>n</sup>)转为了O(n)

((((a4x+a3)x+a2)x+a1)x+a0)

也就是

```
result = a4
result = result * x + a3
result = result * x + a2
result = result * x + a1
result = result * x + a0  --- 注意这里是((((a4x+a3)x+a2)x+a1)x+a0)由内到外呀
```

```js
0 -> a4
↓
99  -> 对应((((a4x+a3)x+a2)x+a1)x+a0)的 a4x
↓
99 × 31 + 97 -> 对应 a4x+a3
↓
(99 × 31 + 97) × 31 + 116 -> 对应 (a4x+a3)x+a2
↓
((99 × 31 + 97) × 31 + 116) × 31 + 115 -> ((a4x+a3)x+a2)x+a1
```

### “霍纳法则”负责的是高效计算；“均匀分布、减少冲突”是哈希函数设计所追求的效果。

### 同时将本来是O(n<sup>n</sup>)转为了O(n)

### `% max` 就是把它限制到数组范围内

### 为什么选择质数？

因为质数的**因子很少**，能够减少一些有规律的数据经过乘法和取模之后产生的周期性碰撞



### key.charCodeAt(0)  这个key的作用是什么啊

- 取 `key` 中下标为 0 的字符，并得到它对应的 Unicode 数字。

- 例如：

  ```
  key = "cat"
  ```

  字符串下标：

  ```
         0    1    2
         ↓    ↓    ↓
        "c"  "a"  "t"
  ```

  所以：

  ```
  key.charCodeAt(0)
  ```

  就是取：

  ```
  "c"
  ```

  然后得到：

  ```
  99
  ```

  因为字符 `c` 的 Unicode 编码是 `99`。

# 一。哈希表

### 1.1.哈希函数封装

- 优秀的哈希函数
  - 快速计算：霍纳法则
  - 均匀分布：质数
    - 长度
    - 幕的底



### 1.2.封装哈希表



### 当三位数组的二维数组添加时this.length不变，为什么this.count要++呢

`this.length` 和 `this.count` 记录的不是同一件事。

- `this.length`：哈希表的容量，也就是可用桶数量。
- `this.count`：实际保存了多少组 `key-value` 数据。

例如：

```
private storage: [string, T][][] = []
private length = 7
private count = 0
```

`length = 7` 表示哈希函数会把 key 映射到 `0 ~ 6` 号桶中。它不因为插入数据而改变，只有扩容或缩容才改变。

假设 `"a"` 和 `"h"` 都映射到第 2 个桶：

```
storage[2] = [
    ["a", 1],
    ["h", 2]
]
```

此时：

```
this.length === 7 // 容量仍为 7 个桶
this.count === 2  // 实际有两条数据
```

所以这段代码：

```
if (!isUpdate) {
    bucket.push([key, val])
    this.count++
}
```

表示：虽然没有新增桶，但确实新增了一条 `key-value` 元组，因此元素总数要加一。





### 1.3.哈希表的插入/修改（put）





### 1.4.哈希表的获取方法（get）





### 1.5.哈希表的删除方法（delete）





### 1.6.哈希表自动扩容（loadFactor）





### 1.7.长度是质数（质数判断/寻找质数）

- 判断是否是质数

  console.log(isPrime(91)); // true，但 91 = 7 * 13，应为 false
  console.log(isPrime(1));  // true，但 1 不是质数，应为 false

​       isPrime(121) // true，但 121 = 11 * 11

所以下面的算法不行，考虑不周全

```js
function isPrime(num:number):boolean {
    let count = 0
    for(let i=2; i<=7;i++) {
        if(num % i === 0) {
            return false
        }
    }

    return true
}
```



# 二。树结构

### 2.1.树结构的概念和相关的特性

- 树（Tree）是一种**非线性**的数据结构：数据元素按层级关系组织，而不是像数组、链表那样排成一条线（即**线性结构**）。
- 一棵树由**有限个**节点组成；它可以为空。非空树必须有且只有一个根节点，其余节点又分别组成若干棵互不相交的子树。

```text
          A                A 是根节点
        / | \              B、C、D 是 A 的子节点
       B  C  D              E、F 是 B 的子节点
      / \
     E   F
```

- 树中没有环。若一个结构存在从某节点出发又能回到该节点的路径，**它不是树。**
- 若树有 `n` 个节点，则边的数量**一**定是 `n - 1`。
- 节点之间没有天然的先后顺序；只有在同一父节点下人为规定从左到右的顺序时，才称为有序树。




### 2.2.树结构的优点和相关的术语

- 树适合表达“**包含、隶属、层级**”关系，例如文件目录、DOM、组织架构、评论回复和菜单。
- **相比链表**，树能**缩短查找路径**；在平衡的搜索树中，查找、插入、删除通常可达到 `O(log n)`。
- **相比数组**，树**不要求元素在物理内存中连续**；**插入或删除节点**通常**不**需要**整体移动元素**。

常用术语：

- **节点（node）**：树中的一个数据元素，如图中的 `B`。
- **根节点（root）**：没有父节点的节点，如 `A`。
- **父节点 / 子节点（parent / child）**：直接**相连且更靠****近根**的一方为**父节点**，另一方为子节点。`A` 是 `B` 的父节点。
- **兄弟节点（sibling）**：拥有同一父节点的节点，如 `B`、`C`、`D`。
- **祖先 / 后代（ancestor / descendant）**：从根到某节点路径上的节点是其祖先；该节点子树中的节点是其后代。`A` 是 `F` 的祖先。
- **叶子节点（leaf）**：没有子节点的节点，如 `C`、`D`、`E`、`F`。
- **节点的度（degree）**：该节点拥有的子节点数量。上图中 `A` 的度为 3，叶子节点的度为 0。
- **树的度**：树中所有节点度的最大值；上图树的度为 3。
- **路径（path）**：由边依次连接的一组节点，如 `A -> B -> F`。
- **路径长度**：路径上**边的数量**。`A -> B -> F` 的路径长度为 2。
- **层级（level）**：通常规定根节点在第 1 层，其子节点在第 2 层，依此类推。
- **深度（depth）**：从**根节点**到**该节点**的**边数**；根节点深度为 0。
- **高度（height）**：从该节点到其最远叶子节点的最长路径的边数；叶子节点高度为 0。树的高度就是根节点的高度。
- **子树（subtree）**：一个节点及其所有后代构成的树。例如以 `B` 为根的部分是一棵子树。






### 2.3.树结构的表示方式（父子/子-兄弟）

- 一棵普通树的同一组层级关系，可以用不同方式保存。常见表示如下。

#### 1. 父子表示

- **父节点数组**：每个节点只保存自己的父节点下标（或引用）。例如：

```text
节点：  A  B  C  D  E  F
下标：  0  1  2  3  4  5
父下标：-1  0  0  0  1  1
```

  `-1` 表示没有父节点，因此下标 `0` 的 `A` 是根节点。通过父下标可以很快向上查找祖先，但要查找一个节点的所有子节点，通常需要遍历整个数组。

- **子节点列表**：每个节点保存一个子节点集合。它更适合从父节点向下遍历，节点度数不固定时也不会预留无用空间。

```ts
type GeneralNode<T> = {
  value: T
  children: GeneralNode<T>[]
}
```

#### 2. 子-兄弟表示（左孩子-右兄弟）

- 普通树的节点可以只保留两个引用：
  - `firstChild`（或 `leftChild`）：指向该节点的第一个子节点；
  - `nextSibling`（或 `rightSibling`）：指向它的下一个兄弟节点。
- 一个节点的其他子节点，通过沿着 `nextSibling` 链逐个找到。例如 `A` 的子节点为 `B、C、D`：`A.firstChild -> B`，`B.nextSibling -> C`，`C.nextSibling -> D`。

```text
普通树：                    子-兄弟表示（只展示指针关系）：
          A                              A
       /  |  \                           |
      B   C   D                         left
     / \                                 B --right--> C --right--> D
    E   F                                |
                                         left
                                         E --right--> F
```

上图右侧的**连接规则**是：(先看图中的B)

```text
二叉树的左指针  = 普通树的第一个子节点
二叉树的右指针  = 普通树的下一个兄弟节点
```

- 这种转换也叫**左孩子-右兄弟法**。它**把任意度的普通树**转换为**一棵二叉树**：每个普通树节点最**多只需要两个指针**，因此可以复用**二叉树的遍历和存储**结构。
- 转换时**不能**把“**一层中所有子节点**都直接接到**左指针**上”；只有**第一个子节点接左指针**，其余子节点必须通过兄弟节点的**右指针串起来**。
- **还原普通树**时，对**某**节点**先**访问其 `left` 链接到的第一个孩子，再沿该孩子的 `right` 链依次取得其余兄弟，即可**恢复原来的子节点列表**。





### 2.4.二叉树的概念和相关的特性 - 术语

- 什么是完全二叉树

- **二叉树（Binary Tree）**是每个节点**最多只有两个子节点**的树。这两个位置有**明确的左右之分**，分别叫左子节点和右子节点；因此二叉树是**有序树**。
- 二叉树可以为空。每个节点的**度**只能是 **`0`、`1` 或 `2`**，不能有三个子节点。
- **第 `i` 层**最多有 `2^(i - 1)` 个节点（根节点为第 1 层）。
- **高度为 `h`** 的二叉树最多有 `2^(h + 1) - 1` 个节点（根节点高度为 0）。此时共有 `h + 1` 层，节点数为 `1 + 2 + 4 + ... + 2^h`。
- 对于 `n` 个节点的非空二叉树，边数仍为 `n - 1`。

二叉树术语：

- **满二叉树（Full Binary Tree）**：每一层都达到最大节点数。若高度为 `h`，节点总数为 `2^(h + 1) - 1`。
- **完美二叉树（Perfect Binary Tree）**：每个非叶子节点都有两个子节点，且所有叶子节点位于同一层。
- **完全二叉树（Complete Binary Tree）**：除**最后一层**外，每一层**节点数**都达到**最大值**；最后一层的节点必须**从左到右连续排列**，**不能**出现“**左边空、右边还有节点**”的空位。

```text
完全二叉树                 不是完全二叉树
      A                         A
    /   \                     /   \
   B     C                   B     C
  / \   /                     \   /
 D   E F                       D E
最后一层按左到右填充         最后一层存在空位后仍有节点
```

- 完全二叉树特别适合用数组存储，因为节点可以按层序连续编号，几乎不会浪费数组下标。堆（Heap）就是典型应用。
- **斜树**：所有节点都只有左子节点或都只有右子节点的二叉树，其高度接近节点数，查找性能会退化。



### 节点总数为 `2^(h + 1) - 1`推导过程

这是等比数列求和。

设：

```
S = 1 + 2 + 4 + ... + 2^h
```

两边同时乘以 `2`：

```
2S = 2 + 4 + 8 + ... + 2^(h + 1)
```

然后用 `2S - S`：

```
2S - S = (2 + 4 + ... + 2^(h + 1))
         - (1 + 2 + 4 + ... + 2^h)

S = 2^(h + 1) - 1
```

因此：

```
1 + 2 + 4 + ... + 2^h = 2^(h + 1) - 1
```




### 2.5.二叉树的存储方式（数组/链表）

- **数组存储（顺序存储）**：按照**层序遍历**的顺序把节点放入数组，**最适合完全二叉树，其他树用链式存储**。

若根节点下标为 `0`，节点下标为 `i`：

```text
左子节点下标：2 * i + 1
右子节点下标：2 * i + 2
父节点下标：Math.floor((i - 1) / 2)，i > 0
```

```text
        A
      /   \
     B     C       =>  [A, B, C, D, E, F]
    / \   /
   D   E F
```

- 优点：**不需要额外保存左右指针**；通过下标即可在 `O(1)` 时间**定位父子节点**；**缓存友好，适合堆**。
- 缺点：普通二叉树，特别是**斜树**，**会产生大量空位，空间利用率低**。

- **链式存储**：每个节点保存数据和指向左右子节点的引用；空子节点用 `null` 表示。（推荐）

```ts
class TreeNode<T> {
  constructor(
    public value: T,
    public left: TreeNode<T> | null = null,
    public right: TreeNode<T> | null = null
  ) {}
}
```

- **优点**：**适用于任意**形状的二叉树，**不会因缺失节点浪费数组空间****；插入、删除时只需调整引用**。
- 缺点：每个节点需要额外保存两个引用，且无法通过下标直接定位任意节点。




### 2.6.二叉搜索树相关的概念和特性

- 二叉搜索树（Binary Search Tree，BST）是一棵二叉树，且对任意节点都满足：

```text
左子树中所有节点的值 < 当前节点的值 < 右子树中所有节点的值
```

- 左右子树也**必须各自是二叉搜索树**，这个规则会递归地作用于整棵树。
- 若允许重复值，需要先约定策略，**例如**“**相等的值**统一放右子树”
- 对 BST 进行**中序遍历**（左 -> 根 -> 右）会得到一个从小到大的**有序序列**。
- 查找、插入、删除都会沿着一条根到叶子的路径进行，时间复杂度为 `O(h)`，`h` 是树高：
  - 树接近平衡时：`h ≈ log n`，操作复杂度约为 `O(log n)`。
  - 树退化成斜树时：`h ≈ n`，操作复杂度退化为 `O(n)`。
- BST **不等于**平衡二叉树。

```text
        8
      /   \
     3    10
    / \     \
   1   6     14
      / \    /
     4   7  13

中序遍历：1, 3, 4, 6, 7, 8, 10, 13, 14
```




### 2.7.二叉搜索树类的封装过程

- 先定义节点类，再在 `BinarySearchTree` 中保存根节点。比较逻辑通过 `compareFn` 注入，使树可存储数字以外的数据。

```ts
class BSTNode<T> {
  constructor(
    public value: T,
    public left: BSTNode<T> | null = null,
    public right: BSTNode<T> | null = null
  ) {}
}

class BinarySearchTree<T> {
  private root: BSTNode<T> | null = null

  constructor(private readonly compareFn: CompareFn<T>) {}
}

const bst = new BinarySearchTree<number>((a, b) => a - b)
;[8, 3, 10, 1, 6, 14, 4, 7, 13].forEach(value => bst.insert(value))
console.log(bst.has(6))    // true
console.log(bst.inOrder()) // [1, 3, 4, 6, 7, 8, 10, 13, 14]
```

封装时的关键步骤：

1. `root` 初始为 `null`；第一次插入时，新节点就是根节点。
2. 后续插入从根节点开始比较：更小则向左，更大则向右，直到找到空位置。
3. 查找采用同样的比较路径；查找失败的条件是走到 `null`。
4. 中序遍历用于验证 BST 性质，结果应当始终有序。
5. 删除是最复杂的操作：叶子节点直接删除；只有一个子节点时用子节点替代它；有两个子节点时，通常用右子树最小节点（中序后继）替代当前节点。



